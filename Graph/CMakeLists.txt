# Fetch .cpp files for library creation
file(GLOB Graph_source
    "*.cpp"
)

# Fetch .h files for library creation
file(GLOB Graph_headers
    "*.h"
)

# target_link_libraries(ophidian_geometry

# )
